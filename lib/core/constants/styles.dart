import 'package:flutter/material.dart';

class Styles {
  static const iconColor = Color.fromRGBO(21, 27, 55, 1);
  static const cardBackgroundColor = Color.fromRGBO(237, 239, 248, 1);
  static final colorScheme =
      ColorScheme.fromSeed(seedColor: const Color.fromRGBO(49, 64, 129, 1));
}
